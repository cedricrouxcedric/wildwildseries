{% extends 'base.html.twig' %}

{% block title %}Actor index{% endblock %}

{% block body %}
    <section class="jumbotron text-center">
        <div class="container">
            <h1 class="text-black-50 font-weight-bold mt-3">Tout les acteurs </h1>
            <p>
                <a href="{{ path('category_index') }}" class="btn btn-primary my-2">Toutes les categories</a>
                <a href="{{ path('wild_index') }}"class="btn btn-success my-2">Toutes les séries</a>

            </p>
        </div>
    </section>
    <div class="container pad-top">
        <h1></h1>
        <table class="table text-white">
            <thead>
            <tr>

                <th>Name</th>
                <th>Slug</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for actor in actors %}
                <tr>

                    <td>{{ actor.name }}</td>
                    <td>{{ actor.slug }}</td>
                    <td>
                        <a href="/wild/actor/{{ actor.name }}">show</a>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <a href="{{ path('actor_new') }}">Create new</a>
    </div>
{% endblock %}
